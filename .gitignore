node_modules/
.env
logs/
*.log
coverage/
data/
# The data/ rule above is for ingest inputs at the repo root; the committed
# coverage registry lives under client/src/data and must not be swept up.
!client/src/data/
# Source documents downloaded for the ingest tools (state registry PDFs
# converted with pdftotext); regenerable from the URLs in each tool's
# SOURCE_URL and documented in docs/research/cannabis-physician-sources.md
tmp/
